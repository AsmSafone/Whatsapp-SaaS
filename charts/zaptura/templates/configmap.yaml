apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zaptura.fullname" . }}
  labels:
    {{- include "zaptura.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
