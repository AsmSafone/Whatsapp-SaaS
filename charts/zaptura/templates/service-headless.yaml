apiVersion: v1
kind: Service
metadata:
  name: {{ include "zaptura.fullname" . }}-headless
  labels:
    {{- include "zaptura.labels" . | nindent 4 }}
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    {{- include "zaptura.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 2785
      targetPort: http
